<%= simple_form_for post do |f| %>
  <%= f.input :title %>
  <%= f.fields_for :sections do |builder| %>
    <%= render 'sections/fields', f:builder %>
  <% end %>
  <%= link_to_add_fields add(:post), f, :sections %>
  <%= f.button :submit %>
<% end %>

en:
  post:
    one: Post
  section:
    one: Section
  tag:
    one: Tag

  labels:
    add: Add %{o}
    delete: Delete %{o}
    edit: Edit %{o}
    new: New %{o}
    view: View %{o}

  successes:
    created: '%{o} created'
    deleted: '%{o} deleted'
    updated: '%{o} updated'

  simple_form:
    labels:
      post:
        sections:
          tag_tokens: Tags
      section:
        tag_tokens: Tags

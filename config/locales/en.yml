en:
  post:
    one: Post
    other: Posts
  section:
    one: Section
    other: Sections
  tag:
    one: Tag
    other: Tags
  user:
    one: User

  labels:
    add: Add %{o}
    delete: Delete %{o}
    edit: Edit %{o}
    new: New %{o}
    view: View %{o}

  successes:
    created: '%{o} created'
    deleted: '%{o} deleted'
    signed_up: '%{o} signed up'
    updated: '%{o} updated'

  helpers:
    submit:
      user:
        create: Signup

  simple_form:
    labels:
      post:
        sections:
          tag_tokens: Tags
      section:
        tag_tokens: Tags
      tag:
        leaf_name: Name
      user:
        password_confirmation: Confirmation

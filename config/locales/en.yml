en:
  post:
    one: Post
    other: Posts
  section:
    one: Section
    other: Sections
  tag:
    one: Tag
    other: Tags

  labels:
    add: Add %{o}
    delete: Delete %{o}
    edit: Edit %{o}
    new: New %{o}
    view: View %{o}

  successes:
    created: '%{o} created'
    deleted: '%{o} deleted'
    updated: '%{o} updated'

  simple_form:
    labels:
      post:
        sections:
          tag_tokens: Tags
      section:
        tag_tokens: Tags
      tag:
        leaf_name: Name
